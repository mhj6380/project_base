import styled from "styled-components";

const RegisterFormWrapper = styled.div`
  width: 100%;
  height: 100%;
  display: flex;
  flex-flow: row wrap;
`;

interface Props {}

const RegisterForm: React.FC<Props> = ({}) => {
  return <RegisterFormWrapper></RegisterFormWrapper>;
};

export default RegisterForm;
